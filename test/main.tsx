import React from "react";
import ReactDOM from 'react-dom/client';
import AffixTest from "./AffixTest";

const root = document.getElementById("root");
root && ReactDOM.createRoot(root).render(<AffixTest />)